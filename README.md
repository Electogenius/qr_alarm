# qr_alarm

A QR-code based alarm clock app for Android (and perhaps iOS)

todo:
- persistent alarm data
- actually schedule alarms
- ringtone picker
- qr to stop alarm
- better qrValue input (via qr instead of text field)